const GetAbiturient = (abiturientId) => new Promise((resolve, reject) => {

});

const AddAbiturient = (abiturientInfo) => new Promise((resolve, reject) => {

});

const AbiturientsProvider = {
  GetAbiturient,
  AddAbiturient,
};

export default AbiturientsProvider;
